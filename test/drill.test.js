const expect = require("mocha").expect
const sort = require("../drill")

describe('Sort function', () => {
    it('should sort the array', () => {

    })

    it('numbers should be ascending', () => {
        
    })
})
